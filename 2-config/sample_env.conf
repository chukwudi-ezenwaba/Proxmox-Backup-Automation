# Environment configuration for the Proxmox backup script

SSD_DEVICE="/dev/sdb3"                # External SSD device (use lsblk to confirm)
MOUNT_POINT="/mnt/external_ssd"       # Mount point for the SSD
SSD_BACKUP_DIR="/mnt/external_ssd/proxmox_backups"
LOG_FILE="/tmp/proxmox_backup_error.log"

# Note: Optional helper script. Run manually:
bash config/sample_mount_check.sh